<%= form_for([@post, @post.comments.build]) do |f| %> 
    
    <p>
        <%= f.label :name %> <br>
        <%= f.text_field :name %> <br>
    
    <p>  
        <%= f.label :body %> <br>
        <%= f.text_area :body %> <br>
    </p>
    
    <p>
        <%= f.submit %> <br>
    </p>

<% end %> 